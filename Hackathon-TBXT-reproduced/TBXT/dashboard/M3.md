# M3 — Boltz-2 co-folding

## Your tasks

| # | Task | When | Effort |
|---|---|---|---|
| 4 | Boltz-2 co-fold on full 570 pool | T-1d 9 am — overnight | ~10 h GPU |

## Done state for team to win

- `report/task4_trial1.json`: `n_ok ≥ 500`, `n_predicted_binders ≥ 50`, CF Labs hits all `affinity_kd_uM < 5`
- Uploaded to Drive by T-1d 11 pm
- If GPU compatibility blocks Boltz: PARTIAL with reference-set numbers from `data/boltz/boltz_summary.csv` (still useful for consensus, just lower coverage)

## Calendar

- **T-1d 9 am**: launch task4 base run; first compound takes 30 min (Boltz weight download)
- **T-1d 11 am**: log shows compound 30/570 complete = on track
- **T-1d 7 pm**: ~half done
- **T-1d 11 pm**: HARD upload — even if 80% complete, upload partial JSON
- **T-12h (May 9 6 am)**: rest finishes overnight; upload final
- **T-12h** if stretch time: launch task4 trial 2 (5 diffusion samples)

## Hard upload deadline

T-1d 11 pm. Partial OK.

## Pointers

- `dashboard/task4_playbook.md`

## Escalation contact

M1 (lead). Boltz environment issues are common — if `cuequivariance` / `nvrtc` errors hit, follow the playbook fix table; if you're in deeper trouble for > 1 h, ping lead for fallback to reference-set partial.

## Why this matters

Boltz `prob_binder` is the single cleanest binder/non-binder discriminator. It's the 4th orthogonal signal in consensus and resolves ties when Vina + GNINA disagree. **Without you, 2 of 4 final picks become coin flips.**
